### 113正式學科

6. 執行下列Python程式片段，其結果為和? (A) 0  (B) 1  (C) 2  (D) 3

```
def calculate(n):
  if n == 0:
    return 0
  return n + calculate(n - 1)

print(calculate(4) % 4)
```

8. 執行下列Python程式片段，其結果為和? (A) YES YES  (B) NO NO  (C) YES NO  (D) NO YES
```
print('NO' if str(101) != str(101)[::-1] else 'YES', end=" ")
print('NO' if str(11) != str(11)[::-1] else 'YES')
```

10. 執行下列Python程式片段，其結果為和? (A) 0  (B) 1  (C) 2  (D) 3

```
def factorial(n):
  if n == 0:
    return 1
  retrun n * factorial(n - 1)
print(factorial(3) % 4 )
```

11. 執行下列Python程式片段，其結果為和? (A) 0  (B) 1  (C) 2  (D) 3
```
x = 5
y = 7
result = (x << 1) + (y >> 1)
print(result % 4)
```

16. 執行下列Python程式片段，其結果為和? (A) 0  (B) 1  (C) 2  (D) 3
```
orders = [100, 150 ,200 ,80 ,120]
avg_orders = sum(orders) / len(orders)
below_avg = [o for o in orders if o < avg_orders]
print(len(below_avg) % 4)
```

17. 執行下列Python程式片段，其結果為和? (A) 0  (B) 1  (C) 2  (D) 3
```
a = [1, 2, 3, 4, 5]
b = [x * 2 for x in a if x % 2 == 0]
print(sum(b) % 4)
```

21. 執行下列Python程式片段，其結果為和? (A) 0  (B) 1  (C) 2  (D) 3
```
def calc(x):
    if x == 0:
        return 4
    return calc(x - 1) + 4
print(calc(3) % 4)
```
23. 執行下列Python程式片段，其結果為和? (A) 0  (B) 1  (C) 2  (D) 3
```
def calc(x):
    if x < 2:
        return x
    return calc(x - 1) + calc(x - 2)

print(calc(5) % 4)
```
